# CI jobs come from .gitlab/ci/grcc.yml (copied verbatim from godot-rust-cross-compiler,
# update with `make sync`). Project-specific overrides go below the include.
include:
  - local: .gitlab/ci/grcc.yml
